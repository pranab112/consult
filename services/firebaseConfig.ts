
// Firebase configuration disabled due to missing modules in environment.
// Falling back to local storage mode.

export const isFirebaseReady = false;

export const auth: any = null;
export const db: any = null;
export const storage: any = null;
